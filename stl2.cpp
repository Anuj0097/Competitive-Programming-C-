#include <bits/stdc++.h>
using namespace std;
int man()
{
    // // vector<int> arr;
    // // arr.push_back(2);
    // // arr.push_back(3);
    // cout << arr.size() << endl;
    vector<int> vec(4, 0);
    cout << vec.size() << endl;
}