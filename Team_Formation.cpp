#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin >> n;
        int m;
        int max;
        string s[n], q[n];
        for (int i = 0; i < n; i++)
        {
            cin >> s[i];
            cin >> q[i];
            m = max(s[i] = '0' || q[i] = '1')
        }
    }